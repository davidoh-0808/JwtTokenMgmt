<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="test.yezac2.global.config.security.auth.mapper.AppUserMapper">

    <select id="getAppUserByUsername" parameterType="String" resultType="test.yezac2.global.config.security.auth.domain.AppUserDetails">
        SELECT
            u.id
--             , u.email AS username
            , u.email
            , u.password
            , u.name
            , u.hospital_id
        FROM
            users u
        WHERE
            u.email = #{username}
    </select>

    <insert id="saveAppUser" parameterType="test.yezac2.global.config.security.auth.dto.JoinReq">
        INSERT INTO users (
            email
            , password
            , name
            , auth_type
            , hospital_id
        ) VALUES (
             #{email}
             , #{password}
             , #{name}
             , #{authType}
             , #{hospitalId}
        )
    </insert>

    <update id="enableAppUser" parameterType="String">
        UPDATE
            users
        SET
            email_verified = TRUE
        WHERE
            email = #{email}
    </update>

</mapper>